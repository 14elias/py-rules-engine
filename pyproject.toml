[project]
name = "rules-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "14elias", email = "ellamebrahtom1995@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
rules-engine = "rules_engine:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
